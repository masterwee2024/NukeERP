# T006 — Responsive Layout Shell

- **Phase**: 0
- **Depends On**: T001, T005
- **Estimated Effort**: 3 days

## What Is Being Built
The application layout: sidebar + header + main content area. Mobile: hamburger button toggles slide-out overlay sidebar. Desktop: persistent sidebar (240px) with collapsible icon-only mode (64px). Global `overflow-x: hidden` enforced. CSS variable theme tokens for light/dark mode.

## How It Will Be Built

### Frontend (React)
- **Layout Component**: `frontend/src/components/Layout/AppLayout.tsx`
  - Sidebar + Header + Content wrapper
  - CSS: `overflow-x: hidden` on `html`, `body`, and all wrapper divs
  - Tailwind: `w-full max-w-full overflow-x-hidden` on layout container
- **Sidebar Component**: `frontend/src/components/Layout/Sidebar.tsx`
  - Reads menu from `useMenu()` hook (dynamic, not hardcoded)
  - Mobile: overlay with backdrop, slide-in/out animation
  - Desktop: fixed position, collapsible to icon-only mode
  - Expandable menu groups (accordion style)
  - Active route highlighting
- **Header Component**: `frontend/src/components/Layout/Header.tsx`
  - Hamburger button (mobile), breadcrumb, user avatar dropdown
  - Notification bell (links to T016 notifications)
  - Company Switcher dropdown (next to user avatar) — shows current company, lists accessible companies, click to switch active company context
- **Theme**: CSS variables in `frontend/src/styles/theme.css`
  - Light/dark mode toggle
  - Color tokens: primary, secondary, background, surface, text, border
- **Responsive breakpoints tested**: 375px, 768px, 1440px

### Components
- `AppLayout.tsx` — main layout wrapper
- `Sidebar.tsx` — navigation sidebar
- `Header.tsx` — top bar
- `SidebarItem.tsx` — individual menu item with icon
- `SidebarGroup.tsx` — expandable menu group

## Deliverables Checklist
- [ ] `AppLayout` component with sidebar + header + content
- [ ] `Sidebar` reads menu from `useMenu()` hook
- [ ] Mobile: hamburger → slide-out overlay sidebar
- [ ] Desktop: persistent sidebar, collapsible to icon-only
- [ ] `Header` with hamburger button, breadcrumb, user dropdown
- [ ] Company Switcher dropdown in header (next to user avatar)
- [ ] Global `overflow-x: hidden` on html, body, wrappers
- [ ] CSS variable theme tokens (light/dark)
- [ ] Active route highlighting in sidebar
- [ ] Expandable menu groups
- [ ] No horizontal scroll at 375px, 768px, 1440px
- [ ] All Tailwind responsive breakpoints working

## Success Criteria
- [ ] Mobile (375px): hamburger visible, sidebar hidden, tap opens overlay
- [ ] Tablet (768px): sidebar visible or collapsible
- [ ] Desktop (1440px): persistent sidebar, no horizontal scroll
- [ ] Sidebar collapses to 64px icon-only mode
- [ ] Menu items from T005 render correctly
- [ ] Active page highlighted in sidebar
- [ ] Header shows current page breadcrumb
- [ ] No horizontal scroll on any page at any width
- [ ] Company switcher shows current company and allows switching
- [ ] Switching company updates all data views on the page
- [ ] All tests pass

## Tests
| Layer | Tool | Test File / Path | Coverage Target |
|---|---|---|---|
| Unit | Vitest | `tests/frontend/unit/AppLayout.test.tsx` | Layout renders, sidebar toggle, responsive behavior |
| Unit | Vitest | `tests/frontend/unit/Sidebar.test.tsx` | Menu items render, groups expand, active highlighting |
| E2E | Playwright | `tests/e2e/specs/layout.spec.ts` | Mobile hamburger works, desktop sidebar persists, no overflow |

### Test Results
| Run ID | Date | Pytest | Vitest | Playwright | Issues | Status |
|---|---|---|---|---|---|---|
| — | — | — | — | — | — | ⬜ |
