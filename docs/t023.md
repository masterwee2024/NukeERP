# T023 — Journal Entry Approval & Posting

- **Phase**: 2
- **Depends On**: T013, T019
- **Estimated Effort**: 3 days

## What Is Being Built
Multi-level approval via T013 workflow before posting. Posting creates immutable GL entries in `GeneralLedger` table. Posted entries cannot be edited — only reversed. Period validation on posting date.

## How It Will Be Built

### Backend (Django)
- **Model** (`apps/financial/models.py`):
  - `GeneralLedger`: journal_entry FK, journal_entry_line FK, account FK, date, debit, credit, balance (running), period FK, company FK
- **Service** (`apps/financial/services/posting_service.py`):
  - `submit_for_approval(journal_entry_id)` — create ApprovalRequest via T013
  - `post_journal_entry(journal_entry_id)` — validate period open, create GL entries, update running balances, set status=posted
  - `reverse_posted_entry(journal_entry_id)` — create reversal JE, post it
- **API** (`apps/financial/api/journal_api.py`):
  - `POST /api/v1/financial/journal-entries/{id}/submit/` — submit for approval
  - `POST /api/v1/financial/journal-entries/{id}/post/` — post (after approval)

## Deliverables Checklist
- [x] `GeneralLedger` model with running balance (pre-existing, enhanced with period FK)
- [x] `FinancialPeriod` model for period management
- [x] Approval integration via T016 workflow engine
- [x] Posting service: period validation, GL entry creation, balance update
- [x] Post and submit API endpoints
- [x] Auto-approve on workflow completion (signal-based)
- [x] Posted entry is read-only (no edit/delete buttons in UI)
- [x] Reversal creates and posts new entry with equal opposite amounts
- [x] `posted_at` exposed in API schema
- [x] Company-scoped security on all mutation endpoints

## Success Criteria
- [x] Submitting JE triggers approval workflow
- [x] Approving (via signal) sets status=approved
- [x] Posting creates GL entries with correct amounts
- [x] Running balance calculated correctly
- [x] Posted JE cannot be edited or deleted
- [x] Reversal JE creates posted entry with opposite amounts
- [x] Cannot post without an open financial period
- [x] All tests pass

## Tests
| Layer | Tool | Test File / Path | Coverage Target |
|---|---|---|---|
| Backend | Pytest | `tests/backend/financial/test_posting.py` (23 tests) | GL creation, balance calculation, period validation, approval flow, reversal, signal |
| Backend | Pytest | `tests/backend/financial/test_journal.py` (20 tests, updated) | Reversal now posts reversal entry |

### Test Results
| Run ID | Date | Pytest (fin) | Django Check | Frontend Build | Issues | Status |
|---|---|---|---|---|---|---|
| 1 | 2026-06-06 | 81/81 ✅ | ✅ | ✅ | — | ✅ |
