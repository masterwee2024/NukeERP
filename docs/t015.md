# T015 — User Management UI

- **Phase**: 1
- **Depends On**: T011
- **Estimated Effort**: 2 days

## What Is Being Built
Admin-facing user management: user list with search/filter, create/edit user form, role assignment, activate/deactivate, admin password reset, login history view.

## How It Will Be Built

### Backend (Django)
- **API** (`apps/core/api/user_api.py`):
  - `GET /api/v1/admin/users/` — list with search, role filter, status filter
  - `POST /api/v1/admin/users/` — create user
  - `PUT /api/v1/admin/users/{id}/` — update user
  - `POST /api/v1/admin/users/{id}/deactivate/` — soft delete
  - `POST /api/v1/admin/users/{id}/reset-password/` — admin password reset
  - `GET /api/v1/admin/users/{id}/login-history/` — login audit trail

### Frontend (React)
- **Pages**:
  - `UserListPage` — table with search, role filter, status filter, quick actions
  - `UserFormPage` — create/edit: name, email, password, roles (multi-select), company, status
  - `UserDetailPage` — profile, roles, login history, audit log

## Deliverables Checklist
- [x] User list API with search, role filter, status filter
- [x] User create/edit API (with role assignment)
- [x] Deactivate API (soft delete, not hard delete, with self-deactivation check)
- [x] Admin password reset API
- [x] Login history API (LoginHistory model + endpoint)
- [x] React user list page (search, role filter, status filter, deactivate/reactivate)
- [x] React user form page with role assignment (toggle pill buttons)
- [ ] React user detail page with login history — deferred (basic edit form covers it)
- [x] All CRUD actions use `useConfirm()` dialog (create, update, deactivate, password reset)
- [ ] Permission-gated: only users with `admin_create` can access — deferred to T014 decorator usage

## Notes
- LoginHistory model created with ip_address, user_agent, device_type, is_successful, login_time
- User form uses single FormState object pattern with useEffect to sync from API
- Role assignment uses toggle pill buttons with existing T014 roles
- Admin routing: `/app/admin/users` (list), `/app/admin/users/new` (create), `/app/admin/users/:id` (edit)
- Mobile responsive: table container has `overflow-x-auto`, header stacks vertically on small screens, reduced padding
- 16 backend tests (LoginHistory model + user API CRUD)
- 168 total backend, 0 lint errors, 0 TS errors, build passes

## Success Criteria
- [x] Admin can create, edit, deactivate users (API + UI)
- [x] Admin can assign multiple roles to a user (API + UI)
- [x] Deactivated user cannot log in (is_active=False, auth check)
- [ ] Password reset sends email notification — deferred (no email infra)
- [x] Login history shows timestamp, IP, device (model + API)
- [x] All destructive actions show confirmation dialog (useConfirm on create/update/deactivate/reset)
- [x] All tests pass (168/168 backend)

## Tests
| Layer | Tool | Test File / Path | Coverage Target |
|---|---|---|---|
| Backend | Pytest | `tests/backend/core/test_user_api.py` | LoginHistory model (2), API endpoints (14) — 16 total |
| Vitest | — | Deferred (no Playwright setup) | — |

### Test Results
| Run ID | Date | Pytest | Vitest | Playwright | Issues | Status |
|---|---|---|---|---|---|---|
| T015-run1 | 2026-06-02 | 168/168 passed | N/A | Not run | — | ✅ |
