# T002 — Docker Compose Dev Environment

- **Phase**: 0
- **Depends On**: T001
- **Estimated Effort**: 3 days

## What Is Being Built
A `docker-compose.yml` that runs the full dev stack: PostgreSQL 16, Redis, Django app server (with hot-reload), React Vite dev server (with hot-reload), and MinIO for file storage. Developers run one command to start everything.

## How It Will Be Built

### Docker Compose Services
- `db`: PostgreSQL 16, persistent volume, health check
- `redis`: Redis 7, health check
- `django`: Python 3.12-slim, mount `./apps` for hot-reload, depends on db + redis
- `frontend`: Node 22-alpine, mount `./frontend` for hot-reload via Vite
- `minio`: MinIO S3-compatible storage, persistent volume
- `pgbouncer`: PgBocker connection pooler (transaction mode, 20 pool size, 500 max client connections)

### Configuration
- `.env` for all secrets (DB password, Redis URL, MINIO keys)
- `docker-compose.override.yml` for dev-specific settings (ports, volumes)
- Django `settings.py` reads from environment via `django-environ`

### Volumes
- `postgres_data`: persistent DB storage
- `minio_data`: persistent file storage
- `./apps:/app/apps` — Django code mount for hot-reload
- `./frontend:/app/frontend` — React code mount for Vite HMR

## Deliverables Checklist
- [ ] `docker-compose.yml` with all 5 services
- [ ] `docker-compose.override.yml` for dev overrides
- [ ] `.env` file with all required variables
- [ ] PostgreSQL 16 with health check
- [ ] Redis 7 with health check
- [ ] Django service with hot-reload
- [ ] React dev server with HMR
- [ ] MinIO accessible on port 9000 (API) and 9001 (console)
- [ ] PgBocker configured (transaction mode, pool size 20, max client 500)
- [ ] Django settings point to PgBouncer (port 6432) not direct DB
- [ ] Nginx configured with HTTP/2 and WebSocket proxy
- [ ] All services start with `docker compose up -d`
- [ ] Persistent volumes survive `docker compose down`

## Success Criteria
- [ ] `docker compose up -d` starts all services within 60 seconds
- [ ] `docker compose exec django python manage.py migrate` succeeds
- [ ] Frontend accessible at `http://localhost:5173`
- [ ] Django backend accessible at `http://localhost:8000`
- [ ] MinIO console accessible at `http://localhost:9001`
- [ ] Code changes in `apps/` trigger Django auto-reload
- [ ] Code changes in `frontend/` trigger Vite HMR
- [ ] PgBouncer accepts connections on port 6432
- [ ] Django connects through PgBouncer (not direct to DB)
- [ ] HTTP/2 enabled on Nginx (verify with curl -I)
- [ ] WebSocket upgrade works for notification endpoint

## Tests
| Layer | Tool | Test File / Path | Coverage Target |
|---|---|---|---|
| — | — | — | Infrastructure setup, not logic |

### Test Results
| Run ID | Date | Pytest | Vitest | Playwright | Issues | Status |
|---|---|---|---|---|---|---|
| — | — | — | — | — | — | ⬜ |
