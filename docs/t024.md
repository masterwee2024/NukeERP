# T024 — Period Management

- **Phase**: 2
- **Depends On**: T010, T020
- **Estimated Effort**: 2 days

## What Is Being Built
Close/reopen fiscal periods. Prevent posting to closed periods. Year-end close process: transfer P&L to retained earnings, lock previous year, open new year.

## How It Will Be Built

### Backend (Django)
- **Service** (`apps/financial/services/period_service.py`):
  - `close_period(period_id)` — validate no unposted entries, set status=closed
  - `reopen_period(period_id)` — set status=open (admin only)
  - `year_end_close(financial_year_id)` — transfer P&L balances to retained earnings, create new financial year with periods, lock old year
- **API** (`apps/financial/api/period_api.py`):
  - `GET /api/v1/financial/periods/` — list with status
  - `POST /api/v1/financial/periods/{id}/close/` — close period
  - `POST /api/v1/financial/periods/{id}/reopen/` — reopen period
  - `POST /api/v1/financial/financial-years/{id}/year-end-close/` — year-end close

## Deliverables Checklist
- [x] `FinancialYear` model for year-level grouping
- [x] Period close/reopen service
- [x] Year-end close with P&L transfer to retained earnings
- [x] Year-end close creates new financial year with 12 monthly periods
- [x] Previous year locked after year-end close
- [x] Period status validation in posting (inherited from T023)
- [x] API endpoints: list, close, reopen, year-end-close, init-periods
- [x] React period management page with year/period tree, close/reopen/year-end-close buttons
- [x] All destructive actions use `useAction()` confirm dialog

## Success Criteria
- [x] Closing a period prevents new postings
- [x] Reopening a period allows postings again
- [x] Period close validates no unposted entries exist
- [x] Year-end close transfers P&L to retained earnings
- [x] Year-end close creates new financial year with 12 periods
- [x] Previous year is locked after year-end close
- [x] All 101 financial tests pass

## Tests
| Layer | Tool | Test File / Path | Coverage Target |
|---|---|---|---|
| Backend | Pytest | `tests/backend/financial/test_period.py` (20 tests) | Close, reopen, year-end transfer, period validation, API |

### Test Results
| Run ID | Date | Pytest (fin) | Django Check | Frontend Build | Issues | Status |
|---|---|---|---|---|---|---|
| 1 | 2026-06-06 | 101/101 ✅ | ✅ | ✅ | — | ✅ |
