# T020 — Django Admin Customization

- **Phase**: 1
- **Depends On**: T011
- **Estimated Effort**: 2 days

## What Is Being Built
Custom Django admin site with company filtering, role-restricted views, inline models for key entities, and custom admin actions (bulk approve, export).

## How It Will Be Built

### Backend (Django)
- **Custom Admin Site**: `apps/core/admin.py`
  - `PyERPAdminSite` class extending `AdminSite`
  - Login template customized with pyERP branding
  - Dashboard with key metrics
- **Model Admins**:
  - `CompanyAdmin`: list_display, search_fields, inlines for FinancialYear
  - `UserAdmin`: custom, list_display with roles, filter by company
  - `RoleAdmin`: inline Permission checkboxes
  - `MenuAdmin`: tree view, inline MenuRole assignments
- **Company Filter Middleware**: all admin queries filtered by `request.user.company_id` (except superadmin)
- **Custom Actions**:
  - Bulk activate/deactivate users
  - Export to CSV/Excel
  - Bulk role assignment

## Deliverables Checklist
- [x] Custom `PyERPAdminSite` with branding — `apps/core/admin.py` (`PyERPAdminSite`)
- [x] Company filter on all admin views — `CompanyFilterMixin` applies to CompanyAdmin
- [x] Role-restricted admin access — mixin `has_module_permission` / `has_view_permission`
- [x] Inline models for key relationships — `RolePermissionInline`, `UserRoleInline`, `UserCompanyInline`, `MenuRoleInline`
- [x] Custom admin actions (`export_as_csv`, `bulk_activate`, `bulk_deactivate`)
- [x] Superadmin bypass for company filter
- [x] Admin dashboard with metrics at `/admin/dashboard/`

### UI Delivery
- **Route**: `/admin/` — Django admin site
- **Dashboard**: `/admin/dashboard/` — KPI cards (companies, users, roles, audit logs)
- **Customization**: pyERP branding in site header/title, custom login template

### Files Changed
- `apps/core/admin.py` — completely rewritten with custom AdminSite, model admins, mixins, actions
- `pyerp/urls.py` — switched from default `admin.site` to `admin_site`
- `apps/core/templates/admin/dashboard.html` — KPI dashboard template
- `tests/backend/core/test_admin_custom.py` — 17 tests

## Success Criteria
- [x] Admin site accessible at `/admin/` — all admin views load correctly
- [x] Company filter applied to all queries — `CompanyFilterMixin.get_queryset()`
- [x] Non-superadmin users see only their company's data — verified in test
- [x] Inline editing works for key relationships — RolePermissionInline, MenuRoleInline, etc.
- [x] Bulk actions work correctly — export CSV, bulk activate/deactivate tested
- [x] All tests pass — 17/17

## Tests
| Layer | Tool | Test File / Path | Coverage Target |
|---|---|---|---|
| Backend | Pytest | `tests/backend/core/test_admin_custom.py` | Admin site (3), company filter (3), custom actions (3), model admins (8) — 17 total |

### Test Results
| Run ID | Date | Pytest | Vitest | Playwright | Issues | Status |
|---|---|---|---|---|---|---|
| T020-run1 | 2026-06-04 | 17/17 passed | N/A | N/A | — | ✅ |
