# T001 — Project Initialization

- **Phase**: 0
- **Depends On**: —
- **Estimated Effort**: 2 days

## What Is Being Built
The pyERP project skeleton: Django backend with Poetry/uv dependency management, React 19 frontend with Vite + TypeScript + TailwindCSS, and a monorepo folder structure that cleanly separates backend, frontend, docs, and tests.

## How It Will Be Built

### Backend (Django)
- Initialize Django project with `django-admin startproject pyerp .`
- Create Django apps: `core`, `financial`, `assets`, `treasury`, `scm`, `crm`, `mrp`, `hrm`, `admin`
- Configure `pyproject.toml` with Poetry/uv, Python 3.12+
- Install core dependencies: django, djangorestframework, django-ninja, psycopg2-binary, redis, celery, django-environ
- Configure `.env` and `settings.py` for database, Redis, ALLOWED_HOSTS, CORS

### Frontend (React)
- `npm create vite@latest frontend -- --template react-ts`
- Install TailwindCSS 4, TanStack Query, TanStack Table, Zustand, React Router, React Hook Form, Zod
- Configure `vite.config.ts` with proxy to Django backend (port 8000)
- Configure TypeScript strict mode

### Project Structure
```
pyERP/
├── apps/                 # Django apps
│   ├── core/
│   ├── financial/
│   ├── assets/
│   ├── treasury/
│   ├── scm/
│   ├── crm/
│   ├── mrp/
│   ├── hrm/
│   └── admin/
├── frontend/             # React app
├── tests/                # Test suites
├── docs/                 # Task details
├── pyproject.toml
├── manage.py
└── .env.example
```

## Deliverables Checklist
- [ ] Django project created with all 9 app directories
- [ ] `pyproject.toml` with all Python dependencies
- [ ] `.env.example` with all required environment variables
- [ ] Django settings configured for PostgreSQL (not SQLite)
- [ ] React + Vite + TypeScript scaffolded in `frontend/`
- [ ] TailwindCSS 4 configured and working
- [ ] React Router configured with basic routes
- [ ] `.gitignore` covers Python, Node.js, Django, IDE files
- [ ] `manage.py` runs without errors

## Success Criteria
- [ ] `poetry run python manage.py migrate` completes
- [ ] `poetry run python manage.py runserver` starts on port 8000
- [ ] `cd frontend && npm run dev` starts on port 5173
- [ ] Frontend loads in browser, shows default page
- [ ] No horizontal scroll on default page
- [ ] All apps listed in `INSTALLED_APPS`

## Tests
| Layer | Tool | Test File / Path | Coverage Target |
|---|---|---|---|
| — | — | — | Project initialization is structural, not logic |

### Test Results
| Run ID | Date | Pytest | Vitest | Playwright | Issues | Status |
|---|---|---|---|---|---|---|
| — | — | — | — | — | — | ⬜ |
