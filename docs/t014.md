# T014 — Role-Based Access Control (RBAC)

- **Phase**: 1
- **Depends On**: T005, T008
- **Estimated Effort**: 3 days

## What Is Being Built
Custom permission model: `Role` → `Permission` (module + action). Assign roles to users. API decorator `@permission_required` enforces access. UI route guards hide unauthorized pages. `MenuRole` junction from T005 links roles to menu visibility.

## How It Will Be Built

### Backend (Django)
- **Models** (`apps/core/models.py`):
  - `Role`: name (unique), description, is_active, is_system (superadmin role cannot be deleted)
  - `Permission`: module (choices: financial/scm/crm/mrp/hrm/admin), action (choices: view/create/update/delete/approve/post/export), codename (auto-generated: `{module}_{action}`)
  - `RolePermission`: role FK, permission FK (unique together)
  - `UserRole`: user FK, role FK (unique together)
- **API** (`apps/core/api/rbac_api.py`):
  - `GET /api/v1/admin/roles/` — list roles with permission count
  - `POST /api/v1/admin/roles/` — create role
  - `PUT /api/v1/admin/roles/{id}/` — update role (name, description)
  - `PUT /api/v1/admin/roles/{id}/permissions/` — assign permissions (list of permission IDs)
  - `PUT /api/v1/admin/users/{id}/roles/` — assign roles to user
  - `GET /api/v1/admin/permissions/` — list all available permissions
- **Decorator**: `@permission_required("financial_approve")` on API views
- **Middleware**: injects `user.permissions` set into request context

### Frontend (React)
- **Hook**: `usePermission()` — check if user has specific permission
- **Route Guard**: `ProtectedRoute` component — wraps routes, redirects if unauthorized
- **RBAC Admin Page**: Role list → select role → checkbox grid of permissions (module × action)
- **User Role Assignment**: User form gains "Roles" multi-select

## Deliverables Checklist
- [x] `Role`, `Permission`, `RolePermission`, `UserRole` models (all inherit ConcurrencyModel)
- [x] Permission codename auto-generation (`Permission.save()` generates `{module}_{action}`)
- [x] Seed default roles: Admin, Finance Manager, Accountant, SCM Manager, Sales, HR Manager, Employee (7 roles)
- [x] Seed all permissions (6 modules × 7 actions = 42 permissions)
- [x] RBAC API endpoints (`/api/v1/core/admin/permissions/`, `/roles/`, `/users/{id}/roles/`)
- [x] `@permission_required` decorator for Django Ninja views (`apps/core/auth/permissions.py`)
- [x] `usePermission()` hook (`hasPermission`, `hasAnyPermission`, `hasRole`)
- [x] `ProtectedRoute` already exists (auth-only, RBAC route guard enhancement deferred)
- [ ] Role admin page with permission checkbox grid — deferred to T015 (User Management UI)
- [ ] User role assignment in user management — deferred to T015

## Notes
- Role admin page and user role assignment UI deferred to T015 (User Management UI builds on T014 backend)
- Frontend route-level RBAC (ProtectedRoute enhancement) deferred to T015
- Menu-level RBAC already exists via T005 MenuRole → auth.Group; T014 adds granular permission system
- 35 backend tests covering models (11), services (12), and API (12)

## Success Criteria
- [x] Creating a role and assigning permissions works (API + service tests)
- [x] Assigning a role to a user grants those permissions (service tests)
- [x] API endpoint without required permission returns 403 (via @permission_required decorator — applied per-endpoint)
- [ ] Frontend hides unauthorized menu items and routes — deferred to T015
- [x] Admin can view/edit all roles and permissions (API endpoints)
- [x] System roles (Admin) cannot be deleted (API validation)
- [x] All tests pass (35/35 backend)

## Tests
| Layer | Tool | Test File / Path | Coverage Target |
|---|---|---|---|
| Backend | Pytest | `tests/backend/core/test_rbac.py` | Models (11), services (12), API (12) — 35 total |
| Unit | Vitest | `tests/frontend/unit/usePermission.test.ts` | Hook returns correct boolean for assigned permissions |
| E2E | Playwright | `tests/e2e/specs/rbac.spec.ts` | Admin assigns role → user sees restricted menus → API returns 403 for unauthorized |

### Test Results
| Run ID | Date | Pytest | Vitest | Playwright | Issues | Status |
|---|---|---|---|---|---|---|
| T014-run1 | 2026-06-02 | 35/35 passed | N/A | Not run (no Playwright setup) | — | ✅ |
| T014-run2 | 2026-06-03 | 35/35 passed | N/A | Not run | — | ✅ |
